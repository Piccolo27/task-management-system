## Initial Command
- npm install
- cp .env.example .env

- Add these two lines in .env <br>
```
PUSHER_APP_KEY=
PUSHER_APP_CLUSTER=
```

## Serve
- npm run dev

Make sure project is running in port 3000
